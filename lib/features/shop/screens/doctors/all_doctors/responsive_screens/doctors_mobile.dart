import 'package:flutter/material.dart';

class DoctorsMobileScreen extends StatelessWidget {
  const DoctorsMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
